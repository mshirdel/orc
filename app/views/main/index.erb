<div class="starter-template">
<div class="row spacer">
	<div class="col-md-3"></div>
	<div class="col-md-6">
		<%= form_tag(controller: "main", action: "search", method: "post") do %>
			<%= label_tag(:q, "Search for:") %>
			<%= text_field_tag(:q ) %>
			<%= submit_tag("Search", class: "btn btn-primary") %>
		<% end %>
	</div>
	<div class="col-md-3"></div>
</div>
</div>

